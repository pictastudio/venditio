meta {
  name: Update product_categories
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/product_categories/:id-or-slug
  body: json
  auth: inherit
}

params:query {
  ~include: tags
}

params:path {
  id-or-slug: 
}

headers {
  Accept: application/json
  Content-Type: application/json
  Locale: {{locale}}
}

body:json {
  {
    "name": "Updated category",
    "abstract": "Updated abstract",
    "metadata": {
      "icon": "diamond"
    },
    "show_in_menu": false,
    "in_evidence": true,
    "tag_ids": [1]
  }
}

docs {
  summary:
  - Update product_categories

  endpoint:
  - method: PATCH
  - path: /product_categories/:id-or-slug

  query params:
  - include (string|array): Optional includes: tags.

  path params:
  - id-or-slug: Replace with target resource identifier.

  body:
  - JSON payload.
  - Available fields:
  - parent_id
  - name
  - slug
  - abstract
  - description
  - metadata (array)
  - img_thumb (object|nullable)
  - img_thumb.file (file|image, required_with:img_thumb)
  - img_thumb.alt
  - img_thumb.name
  - img_cover (object|nullable)
  - img_cover.file (file|image, required_with:img_cover)
  - img_cover.alt
  - img_cover.name
  - active
  - show_in_menu
  - in_evidence
  - sort_order
  - tag_ids (array)
  - tag_ids.*
  - visible_from
  - visible_until
  - {locale}.name / name:{locale}
  - {locale}.slug / slug:{locale}
  - {locale}.abstract / abstract:{locale}
  - {locale}.description / description:{locale}
  - Note: for image upload use multipart/form-data.
}
